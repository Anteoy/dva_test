import React from 'react';
import styles from './GError.css';
import { Toast } from 'antd-mobile';

function GError() {
  return (
    <div className={styles.normal}>
      Component: GError
    </div>
  );
}

export default GError;
